import {CarouselProps} from "./types";

export const defaultProps: CarouselProps = {
	isMobile: false, // Do we need it?
	slidesToScroll: 1,
	focusOnScroll: false,
	speed: 500,
	dots: false,
	arrows: false,
};